import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  /// Primary brand color
  static const Color primary = Color(0xFFDC143C);

  /// Background color
  static const Color background = Color(0xFFFFFFFF);

  /// Main text color (headings, titles)
  static const Color mainText = Color(0xFF1C1C1C);

  /// Secondary text color (body text)
  static const Color text = Color(0xFF6E6E6E);

  /// Hint / placeholder text color
  static const Color hintText = Color(0xFFBDBDBD);

  /// Input field background color
  static const Color inputField = Color(0xFFF2F0EF);
}
